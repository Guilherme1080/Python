def clt():  
    hora = 50
    hora = hora
    horas = float(input("quantas horas vc trabalhou: "))
    if horas > 40:
        total = hora
        total = hora + 25
        calculo = total
        calculo = total * horas
        print(f"seu salario é {calculo}")
    elif horas <= 40:
        res = hora
        res = hora * horas
        print(f"seu salario é {res}")
clt()